<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Set background color to transparent -->
    <corners android:radius="30dp" /> <!-- Adjust the radius to your preference for rounded corners -->
    <stroke android:width="2dp" android:color="@color/black" /> <!-- Optional: Add a border if desired -->
</shape>